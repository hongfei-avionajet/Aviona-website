# Required server-only SMTP credentials. Never prefix these with VITE_.
SMTP_USER=ops@avionajet.com
SMTP_PASSWORD=

# Alibaba Mail SSL defaults used by api/contact.js.
SMTP_HOST=smtp.qiye.aliyun.com
SMTP_PORT=465
SMTP_SECURE=true
